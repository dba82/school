/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FormattedTextInputComponent } from './formatted-text-input.component';

describe('Component: FormattedTextInput', () => {
  it('should create an instance', () => {
    let component = new FormattedTextInputComponent();
    expect(component).toBeTruthy();
  });
});
