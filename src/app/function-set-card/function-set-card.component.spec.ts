/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FunctionSetCardComponent } from './function-set-card.component';

describe('Component: FunctionSetCard', () => {
  it('should create an instance', () => {
    let component = new FunctionSetCardComponent();
    expect(component).toBeTruthy();
  });
});
